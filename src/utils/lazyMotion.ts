// src/utils/lazyMotion.ts
import { domAnimation, LazyMotion, motion } from "framer-motion";

// LazyMotion sadece `features`'ı props olarak bekler.
// motion.div, motion.section gibi bileşenler JSX ile birlikte kullanılır.
export { LazyMotion, domAnimation, motion };
